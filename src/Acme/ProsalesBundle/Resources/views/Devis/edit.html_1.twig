{% extends '::base.html.twig' %}

{% block body -%}
{{ form_start(edit_form) }}
    {# render the task's only field: description #}
    {{ form(edit_form) }}

    <h3>LignesDevis</h3>  
    <ul class="tags">
        {# iterate over each existing tag and render its only field: name #}
        {% for lignedevis in edit_form.lignesdevis %}
            <li>{{ form_row(lignedevis.quantite) }}</li>
        {% endfor %}
    </ul>
{{ form_end(edit_form) }} 
    <ul class="record_actions">
        <li>
        <a href="{{ path('devis') }}">
            Back to the list
        </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
