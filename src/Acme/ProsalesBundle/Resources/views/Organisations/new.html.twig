{% extends '::base.html.twig' %}

{% block body -%}
    {{ form_start(form, {'attr': {'onsubmit':'return sendForm("POST",this);'}}) }} 
        {{ form_errors(form) }}  
        {{ form(form) }}
    {{ form_end(form) }}  
{% endblock %}
