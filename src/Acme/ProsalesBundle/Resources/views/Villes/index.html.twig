{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Villes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ville</th>
                <th>Cp</th>
                <th>Pays</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('villes_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ville }}</td>
                <td>{{ entity.cp }}</td>
                <td>{{ entity.pays }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('villes_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('villes_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('villes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
